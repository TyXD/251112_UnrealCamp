# 🌐 [내일배움캠프 언리얼엔진] 멀티플레이의 심장: 서버 구조와 통신 원리

> ## **학습 키워드**
>
> *   `P2P (Peer-to-Peer)`: 모든 클라이언트가 서버 역할을 겸하는 구조.
> *   `Listen Server (리슨 서버)`: 플레이어 중 한 명이 호스트(서버)가 되는 구조.
> *   `Dedicated Server (데디케이티드 서버)`: 게임 플레이에 관여하지 않는 전용 서버 컴퓨터를 사용하는 구조.
> *   `GameMode`: 게임의 규칙을 정의하며, **서버에만 존재**하는 핵심 클래스.
> *   `GameState`: 게임의 전역 상태(점수, 시간 등)를 저장하며, 모든 클라이언트에 **복제**되는 클래스.
> *   `PlayerController`: 플레이어의 입력을 처리하는 '영혼'과 같은 존재로, 각 클라이언트는 자신만의 것을 가짐.
> *   `Replication (복제)`: 서버의 정보를 클라이언트에 동기화하는 과정.

<br>

---

## **1. 서버 구조의 종류: P2P, 리슨, 그리고 데디케이티드**

---

멀티플레이어 게임을 개발하기 위해 가장 먼저 이해해야 할 것은 바로 **서버 구조**입니다. 

어떤 구조를 선택하느냐에 따라 게임의 안정성, 확장성, 비용이 크게 달라집니다.

### 💻 **P2P (Peer-to-Peer) 서버**

> P2P 구조에서는 별도의 중앙 서버 없이, **모든 참여자가 클라이언트이자 서버**의 역할을 동시에 수행합니다.
> 
> *   **대표적인 예시**: 토렌트, 다크 소울 시리즈의 매칭 시스템
> *   
> *   **특징**: 중앙 서버 비용이 들지 않지만, 각 참여자의 컴퓨터 자원과 네트워크 환경에 크게 의존합니다.
> *
> *   참여자가 많아질수록 안정성이 떨어질 수 있습니다.

### 🎧 **리슨(Listen) 서버**

> 참여자 중 **한 명이 서버 역할(호스트)을 겸하고, 나머지 참여자들은 클라이언트(게스트)로 접속**하는 구조입니다.
> 
> *   **대표적인 예시**: 마인크래프트, 어몽어스
> *   
> *   **특징**: 소규모 멀티플레이 게임을 빠르고 간단하게 구축할 수 있습니다.
> *
> *   하지만 호스트의 컴퓨터 성능이나 네트워크가 불안정하면 모든 플레이어의 게임 경험에 영향을 미치며,
> *
> *   호스트가 게임을 종료하면 세션이 사라집니다.

### 🚀 **데디케이티드(Dedicated) 서버**

> 게임 플레이에 직접 참여하지 않는, **오직 서버 역할만을 전담하는 별도의 컴퓨터**를 두는 구조입니다.
>
> 흔히 '서버-클라이언트' 모델이라고 부릅니다.
> 
> *   **대표적인 예시**: 배틀그라운드, 리그 오브 레전드 등 대부분의 대규모 온라인 게임
> *   **특징**: 24시간 안정적인 운영이 가능하고, 모든 클라이언트에게 공평한 환경을 제공할 수 있어
> *
> *   대규모 멀티플레이 게임에 가장 이상적입니다. 언리얼 엔진은 이 데디케이티드 서버 구조를 강력하게 지원합니다.

<br>

---

## **2. 언리얼 엔진의 데디케이티드 서버 실행 흐름**

---

언리얼 엔진에서 데디케이티드 서버가 어떻게 시작되고 클라이언트를 맞이할 준비를 하는지 그 과정을 살펴보겠습니다.

1.  **서버 프로세스 실행**
    > 서버 전용 실행 파일(`.exe`)을 실행하거나, 에디터 내에서 PIE(Play In Editor)로 서버를 시작합니다.

2.  **레벨 로드 및 리슨(Listen) 상태 진입**
    > 서버가 실행되면 내부적으로 `open [레벨 이름]?listen` 명령이 수행됩니다.
    >
    > 이는 지정된 레벨을 로드하고, 클라이언트의 접속을 받을 수 있도록 소켓을 여는 과정입니다.

3.  **핵심 클래스 스폰**
    > 레벨 로드가 완료되면, 서버는 해당 레벨의 **World Settings**나 **Project Settings**에 지정된 클래스들을 메모리에 생성(스폰)합니다.
    > *   `GameMode`: 게임의 규칙을 정의합니다. **매우 중요한 점은, GameMode는 오직 서버에만 존재한다는 것입니다.**
    > *   `GameState`: 게임의 전역 상태를 관리합니다.

이 단계가 끝나면, 서버는 모든 규칙과 상태를 가지고 클라이언트의 접속을 기다리는 준비된 상태가 됩니다.

<br>

---

## **3. 클라이언트의 접속과 동기화(Replication) 과정**

---

1.  **접속 요청**: 클라이언트는 서버의 IP 주소와 포트 번호를 통해 서버에 접속을 요청합니다.
2.  **레벨 정보 전송**: 서버는 접속을 허가하고, 현재 플레이 중인 레벨 정보를 클라이언트에게 전송합니다.
3.  **클라이언트 레벨 로드**: 클라이언트는 전달받은 정보로 해당 레벨을 로드하고, 준비가 완료되었음을 서버에 알립니다.
4.  **플레이어 관련 액터 스폰 및 복제**:
    > 서버는 해당 클라이언트를 위한 `PlayerController`, `PlayerCharacter`, `PlayerState` 등의 액터를 **서버 월드에 생성(원본)**합니다.
    >
    > 그리고 이 액터들의 정보를 클라이언트에게 전송하여, 클라이언트의 월드에도 동일한 액터들이 **복제본(Proxy)**으로 생성되도록 합니다.

5.  **GameState 복제**: `GameState` 역시 서버의 정보가 클라이언트에 그대로 복제되어, 모든 플레이어가 동일한 게임 상태(점수, 남은 시간 등)를 공유하게 됩니다.

> 💡 **원본(Authority)과 복제본(Proxy)**
>
> 멀티플레이 환경에서 모든 게임 로직의 '진짜'는 서버에 있는 원본 액터입니다.
>
> 클라이언트에 있는 것은 서버의 정보를 그대로 따라하는 '복제된 아바타'일 뿐입니다.
>
> 이 개념이 바로 **리플리케이션(Replication)**의 핵심입니다.

<br>

---

## **4. 액터의 분포: 누가 무엇을 가지는가?**

---

서버와 클라이언트 간에 모든 액터가 동일하게 존재하는 것은 아닙니다. 각 액터의 역할에 따라 분포가 다릅니다.

*   **`GameMode`**: 👑 **오직 서버에만** 존재합니다.
*   **`GameState`**: 🌍 서버에 원본이 있고, **모든 클라이언트에 복제본**이 존재합니다.
*   **`PlayerController`**: 👻 서버에는 **모든 플레이어의 컨트롤러**가 존재하지만, 각 클라이언트에는 **자기 자신의 컨트롤러만** 존재합니다. 플레이어의 '영혼'과 같아서, 남의 영혼까지 알 필요는 없습니다.
*   **`PlayerCharacter`**: 🧍 서버에 모든 플레이어의 원본 캐릭터가 존재하고, 각 클라이언트에는 **자신의 원본(조종 권한을 가진) 캐릭터**와 **다른 플레이어들의 복제본 캐릭터**가 존재합니다.

<br>

---

## **5. 서버-클라이언트 통신의 황금률**

---

> **클라이언트와 클라이언트는 절대로 직접 통신할 수 없습니다.**

멀티플레이 통신에서 가장 중요한 원칙입니다. 모든 정보 교환은 반드시 서버를 거쳐야 합니다.

> **Client A ➡️ Server ➡️ Client B**

클라이언트 A가 B에게 메시지를 보내고 싶다면, A는 서버에게 메시지를 보내고, 서버가 그 메시지를 검증한 뒤 B에게 전달하는 방식을 따릅니다. 

이 구조는 앞으로 배우게 될 **RPC(Remote Procedure Call)**와**프로퍼티 리플리케이션(Property Replication)** 같은 

네트워크 개념을 이해하는 데 가장 기초가 되는 핵심 원리입니다.

---

#언리얼엔진 #멀티플레이 #서버 #네트워크 #게임개발 #내일배움캠프
